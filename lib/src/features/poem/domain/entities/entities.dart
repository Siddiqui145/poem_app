// 1st Step, just defined the correct structure without json or APIs
    class PoemEntity {
        final String title;
        final String author;
        final List lines;

        PoemEntity({
          required this.title,
          required this.author,
          required this.lines,
        });
    }
    